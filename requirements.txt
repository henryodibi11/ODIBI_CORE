# ODIBI CORE v1.0 - Core Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies (required)
pandas>=1.5.0
typing-extensions>=4.0.0

# Engine dependencies (choose based on usage)
duckdb>=0.9.0        # For PandasEngineContext SQL support
pyspark>=3.3.0       # For SparkEngineContext

# Format support (optional)
pandavro>=1.7.0      # For AVRO format
sqlalchemy>=2.0.0    # For SQL database connections (PostgreSQL, MySQL, etc.)
pyodbc>=4.0.0        # For SQL Server connections
psycopg2-binary>=2.9.0  # For PostgreSQL connections (alternative to psycopg2)

# Function library dependencies (optional, for Phase 7+)
iapws>=1.5.0         # For thermodynamic calculations

# Cloud dependencies (Phase 7 - Azure integration)
azure-identity>=1.16,<2                    # Azure authentication (managed identity, service principal)
azure-storage-file-datalake>=12.14,<13     # Azure Blob Storage / ADLS Gen2
azure-keyvault-secrets>=4.7,<5             # Azure Key Vault for secrets management
# pyodbc>=4.0.0 - Already listed above for SQL Server (optional for Azure SQL)

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
black>=23.0.0
mypy>=1.0.0
pandas-stubs>=1.5.0

# Optional: Jupyter for interactive development
ipython>=8.0.0
jupyter>=1.0.0

# Phase 10: LearnODIBI Studio dependencies
streamlit>=1.30.0
plotly>=5.18.0
watchdog>=4.0.0        # File watching for Streamlit hot reload
